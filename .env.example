# API URL baked into the agent at build time. Copy to .env.local (gitignored).
# For a VM or another PC, use this machine's LAN IP and run the API with `npm run dev -- --host`.
CTRLALTBRO_API_URL=http://localhost:5173
